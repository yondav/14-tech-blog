
      <form class="col-lg-12 col-md-12 col-sm-12 col-xs-12 post-title edit-comment hide">
        <div class="d-flex">

          <span class="yellow">Comment</span>
          <span class="teal">.</span>
          <span class="blue">update</span>
          <span class="pink">()</span>
          <span class="teal">;</span>
        </div>
        <div class="d-flex justify-content-between">
          <textarea class="form-control comment-body" data-user="{{userId}}"></textarea>
          <button type="submit" class="btn btn-outline-success post-btn">Update</button>
        </div>
      </form>
